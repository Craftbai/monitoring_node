# 问题总结（FAQ）

本文档汇总项目开发过程中讨论过的重要技术问题，按序号记录。

---

## 1. 电流传感器为什么没看到？看到的是 ADC？

**答**：项目确实有电流传感器（ACS712），只是它的输出是**模拟电压**，需要 STM32 的 **ADC 读取**。

完整链路：

```
被测电流 → ACS712 传感器 → 模拟电压 → STM32 ADC → 数字值 → 软件计算 → 电流值
(0-5A)     (霍尔效应)     (0-5V)    (读取)     (0-4095)  (算法)    (mA)
```

- **ACS712**：霍尔电流传感器，输出 0-5V 模拟电压
- **ADC+DMA**：把模拟电压变成数字值（1 kHz × 1024 点）
- **软件**：零点标定 + 比例换算，得到 mA

---

## 2. NRF24L01 无线模块有什么用？为什么加这个？

**答**：NRF24L01 实现**无线数据上报**，适合**分布式监测**场景。

工厂里多台设备分散、移动、难以布线，需要低功耗无线方案。NRF24L01 的优势：

- ✅ 功耗低（发送 ~13 mA，待机 ~1 μA）
- ✅ 距离远（空旷 100 米）
- ✅ 成本低（~5 元）
- ✅ 易组网（一个接收器收集多个节点）

项目采用**双通道上报**：UART 用于调试，NRF24 用于实际部署。

---

## 3. DS18B20 用 1-Wire 而不是 I2C/SPI 是什么意思？

**答**：1-Wire 是**单总线通信协议**，只需 1 根数据线即可双向通信。

对比：

| 协议 | 引脚数 | 速度 | 距离 |
|------|--------|------|------|
| SPI | 4 | MHz | 1 m |
| I2C | 2 | 400 kHz | 5 m |
| 1-Wire | 1 | 16 kbps | 100 m |

DS18B20 温度变化慢，用 1-Wire 只需 1 根引脚，节省资源，且支持远距离布线。

---

## 4. I2C 不是一主多从吗？为什么说被 NRF24 占用？

**答**：这是之前表述不准确。I2C 确实是一主多从，理论上可以连接多个设备。

实际上项目的引脚分工是：

- **I2C1（PB8/PB9）** → MPU6050 振动传感器
- **SPI2（PB13/14/15）** → NRF24L01 无线模块
- **1-Wire（PG11）** → DS18B20 温度传感器

DS18B20 用 1-Wire 是**设计选择**（省引脚、远距离），不是因为 I2C "被占用"。

---

## 5. SPI 也是多从吧？还是只能一对一？

**答**：SPI 是**一主多从**，但每个从设备需要**独立的 CS（片选）引脚**。

- SCLK/MOSI/MISO 三条线共享
- 每个从设备额外占用 1 个 GPIO 做 CS

对比（N 个设备的总引脚数）：

| 协议 | 共享线 | 额外引脚 | 总引脚数 |
|------|--------|---------|---------|
| SPI | 3 | 1/设备 | 3 + N |
| I2C | 2 | 0 | 2 |
| 1-Wire | 1 | 0 | 1 |

所以多设备时，I2C 和 1-Wire 更省引脚。

---

## 6. 为什么用队列传递周期请求，而不是信号量？

**答**：因为需要**传递数据**，不只是通知。

CycleTask 需要告诉 AcquisitionTask：
- cycle_id（周期 ID，用于日志关联）
- timestamp_ticks（周期开始时间戳）

信号量只能"通知事件"，不能携带数据；如果用信号量，只能靠全局变量，不安全。

**选择规则**：
- 只通知事件（无数据）→ 用信号量
- 需要传递数据 → 用队列

项目里只有 `report_done_sem` 用信号量（只需通知"上报完成"，无数据），其余都用队列。

---

## 7. defaultTask 优先级 24 会不会导致 ReportTask 无法完成？

**答**：不会。defaultTask 虽然优先级（24）高于 ReportTask（16），但它**几乎一直在阻塞**。

defaultTask 只做两件事：翻转 LED + `osDelay(500)`。实际运行时间 < 1 μs，阻塞 500 ms。

```
defaultTask: [翻转LED<1μs] [延时500ms阻塞] [翻转LED<1μs] [延时500ms阻塞]...
ReportTask:  ━━━━━━━━━━━[运行100ms]━━━━━━━━━━━━━━━
                  ↑ 几乎不受影响
```

所以对上报任务没有实质影响。

---

## 8. AcquisitionTask 为什么会阻塞？队列满了吗？

**答**：是**队列空**，不是队列满。

AcquisitionTask 的循环：

```c
for (;;) {
  osMessageQueueGet(g_cycle_request_queue, &request, osWaitForever); // 队列空→阻塞
  // 采集...
  osMessageQueuePut(g_sample_ready_queue, &block, ...);
  // 循环回来，如果队列还是空，继续阻塞
}
```

完成一个周期后，再次调用 `Get` 时，cycle_request_queue 是空的（CycleTask 还没发新请求），任务阻塞等待。

- **队列空** → Get 阻塞（等待数据）
- **队列满** → Put 阻塞（等待空位）

阻塞是正常的**事件驱动**设计，任务没事做就主动让出 CPU，节省功耗。

---

## 9. 为什么选择 Stop 模式而不是 Sleep 或 Standby？

**答**：Stop 模式是**唯一同时满足**"低功耗 + RTC 唤醒 + SRAM 保留"三个需求的模式。

STM32F103 三种低功耗模式对比：

| 模式 | 功耗 | RTC 运行 | SRAM 保留 | 唤醒后状态 |
|------|------|---------|----------|-----------|
| Sleep | ~10 mA | ✅ | ✅ | 保留 |
| **Stop** | **15-50 μA** | ✅ | ✅ | **保留** |
| Standby | 2-5 μA | ✅ | ❌ 丢失 | 复位 |

**为什么不用 Sleep**：功耗太高（~10 mA），300 秒周期下电池撑不住。

**为什么不用 Standby**：SRAM 丢失，唤醒等于复位，连续告警计数会清零。

关键原因——**连续告警需要跨周期保留计数**：

```c
// 连续 3 次超限才告警
if (g_over_count[index] < MONITOR_ALERT_CONFIRM_CYCLES) {
  g_over_count[index]++;  // 必须跨周期保留！
}
```

- Stop 模式：SRAM 保留，周期 1→2→3 连续计数，最终触发告警 ✅
- Standby 模式：每次唤醒计数清零，永远达不到"连续 3 次"，告警永不触发 ❌

**结论**：Stop 模式功耗够低、RTC 能闹钟唤醒、SRAM 保留不丢告警状态，是周期性低功耗监测的唯一正确选择。

---

## 10. 为什么任务用静态创建而不是动态创建？

**答**：静态分配是嵌入式系统的最佳实践，特别是安全关键系统。

| 特性 | 静态创建 | 动态创建 |
|------|---------|---------|
| 内存分配时机 | 编译时 | 运行时 |
| 失败风险 | 编译时报错 | 运行时可能失败 |
| 内存碎片 | 无 | 可能有 |
| 确定性 | 100% | < 100% |

项目任务数固定（6 个），运行期间不销毁，静态创建更安全、更确定，符合 DO-178C/IEC 61508 安全标准要求。

# 传感器与无线模块详解

## 目录
1. [电流传感器（ACS712）](#电流传感器acs712)
2. [无线模块（NRF24L01）](#无线模块nrf24l01)
3. [为什么需要这些模块](#为什么需要这些模块)

---

## 电流传感器（ACS712）

### ❓ 为什么看到的是 ADC 而不是电流传感器？

**简答**：您**确实有电流传感器**（ACS712），只是它输出的是**模拟电压**，需要 STM32 的 **ADC 读取**。

### 📐 完整的电流测量链路

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  被测电流    │ ──>│  ACS712      │ ──>│  STM32       │ ──>│  软件算法    │
│  (风扇/电机) │    │ 霍尔传感器   │    │   ADC1       │    │  (计算电流)  │
│              │    │              │    │              │    │              │
│  0-5A DC     │    │  0-5V 模拟   │    │  0-4095      │    │  0-5000 mA   │
│  (物理电流)  │    │  (电压信号)  │    │  (数字值)    │    │  (最终结果)  │
└──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘
      ⚡                   📶                  🔢                  📊
   直流电流             模拟电压            ADC 读数            电流值
```

### 🔧 ACS712 工作原理

**霍尔效应传感器**：
- **输入**：通过传感器的电流产生磁场
- **转换**：霍尔元件检测磁场强度
- **输出**：磁场强度转换为电压信号（0-5V）
- **隔离**：电气隔离，安全可靠

**关键特性**：
- ✅ **非侵入式测量**：不需要断开电路
- ✅ **电气隔离**：测量侧与控制侧隔离
- ✅ **双向测量**：可测量正负电流
- ✅ **快速响应**：带宽 80 kHz

### 📊 电压-电流对应关系

ACS712 输出电压与电流的关系：

| 电流 (A) | 输出电压 (V) | ADC 值 (12位) | 说明 |
|---------|-------------|--------------|------|
| -5.0    | 0.0         | 0            | 最大反向电流 |
| 0.0     | 2.5         | 2048         | **零点**（无电流） |
| +1.0    | 2.685       | 2151         | 1A 正向电流 |
| +2.5    | 2.968       | 2378         | 2.5A 正向电流 |
| +5.0    | 5.0         | 4095         | 最大正向电流 |

**实际项目配置**（见 `monitoring_config.h`）：
```c
#define MONITOR_CURRENT_ZERO_RAW         2068U   // 零点 ADC 值（标定后）
#define MONITOR_CURRENT_COUNTS_PER_AMP   83U     // 每安培对应的 ADC 增量
```

**零点偏移**：
- 理论零点：2048（2.5V @ 3.3V 参考）
- 实际零点：2068（标定后，考虑传感器个体差异）

### 🧮 电流计算公式

```c
/* 代码实现（见 monitoring_algorithm.c） */

// 1. 读取 1024 个 ADC 样本（1 kHz × 1 秒）
uint16_t adc_samples[1024];

// 2. 计算均值
uint32_t sum = 0;
for (int i = 0; i < 1024; i++) {
  sum += adc_samples[i];
}
uint16_t mean_adc = sum / 1024;  // 例如：2151

// 3. 计算零点偏移
int32_t offset = mean_adc - MONITOR_CURRENT_ZERO_RAW;  // 2151 - 2068 = 83

// 4. 转换为电流（mA）
int32_t current_ma = (offset * 1000) / MONITOR_CURRENT_COUNTS_PER_AMP;
// = (83 × 1000) / 83 = 1000 mA = 1A
```

### 🔌 硬件连接

```
ACS712 传感器模块
┌─────────────────────┐
│  VCC  ──────> 5V    │  供电
│  GND  ──────> GND   │  地
│  OUT  ──────> PA0   │  模拟输出 → STM32 ADC1_IN0
│                     │
│  IP+  ◄───── 被测   │  电流输入端（串联到负载）
│  IP-  ─────► 回路   │  电流输出端
└─────────────────────┘

风扇/电机电路：
  +12V ───> [IP+] ACS712 [IP-] ───> 风扇 ───> GND
                    │
                   OUT (模拟电压)
                    │
                    └──────> STM32 PA0 (ADC1_IN0)
```

### 🎯 为什么使用 ADC + DMA？

**问题**：如果用普通 ADC 轮询采样会怎样？

```c
/* ❌ 不好的方式：轮询 */
for (int i = 0; i < 1024; i++) {
  HAL_ADC_Start(&hadc1);
  HAL_ADC_PollForConversion(&hadc1, 100);
  samples[i] = HAL_ADC_GetValue(&hadc1);
  HAL_Delay(1);  // CPU 空转等待！浪费 1024 ms
}
```

**优势**：DMA 自动搬运，CPU 可以做其他事

```c
/* ✅ 好的方式：DMA + 定时器触发 */
HAL_ADC_Start_DMA(&hadc1, (uint32_t*)g_adc_buffer, 1024);
HAL_TIM_Base_Start(&htim3);  // TIM3 以 1 kHz 触发 ADC

// CPU 继续执行其他任务，DMA 自动完成 1024 次采样
// 1.024 秒后，DMA 完成中断通知 CPU
```

**性能对比**：

| 方法 | CPU 占用 | 采样精度 | 功耗 |
|------|---------|---------|------|
| 轮询 | 100%（1秒卡死） | 不精确 | 高 |
| DMA | < 1%（后台搬运） | 精确 1 kHz | 低 |

---

## 无线模块（NRF24L01）

### ❓ 为什么要加无线模块？

**答**：实现**无线数据上报**，适合**分布式监测**场景。

### 🏭 实际应用场景

想象一个工厂车间：

```
工厂车间布局：
┌────────────────────────────────────────────────────────────┐
│                                                            │
│  设备1 📍    设备2 📍    设备3 📍    设备4 📍    设备5 📍   │
│  (监测节点)   (监测节点)   (监测节点)   (监测节点)   (监测节点) │
│   电池供电     电池供电     电池供电     电池供电     电池供电   │
│                                                            │
│        所有节点通过 NRF24L01 无线上报数据                     │
│                      │                                     │
│                      ▼                                     │
│                  🖥️ 中心监控站                              │
│              (接收所有节点数据)                              │
│           实时显示所有设备状态                               │
└────────────────────────────────────────────────────────────┘
```

**如果不用无线模块，会怎样？**

| 方案 | 问题 |
|------|------|
| **有线（UART/RS485）** | ❌ 布线困难（设备可能移动、高处、难以触及） |
| **WiFi** | ❌ 功耗太高（~100 mA），电池撑不了几天 |
| **蓝牙** | ❌ 距离短（~10 米），不适合大场地 |
| **4G/LoRa** | ❌ 成本高，需要运营商或专用网关 |

**NRF24L01 的优势**：
- ✅ **低功耗**：发送 ~13 mA，待机 ~1 μA
- ✅ **距离远**：空旷环境 100 米，室内 30-50 米
- ✅ **成本低**：~5 元/模块
- ✅ **易组网**：一个接收器可收集多个节点
- ✅ **无需网关**：点对点或星型拓扑

### 📡 NRF24L01 技术参数

| 参数 | 值 |
|------|---|
| **频段** | 2.4 GHz ISM 频段（免授权） |
| **通信距离** | 空旷 100 米，室内 30-50 米 |
| **数据速率** | 250 kbps / 1 Mbps / 2 Mbps |
| **发射功率** | 0 dBm（可调） |
| **电流消耗** | 发送 13.5 mA，接收 13.3 mA，待机 1 μA |
| **接口** | SPI（10 MHz 最大） |
| **数据包大小** | 1-32 字节 |
| **通道数** | 126 个（2.4-2.525 GHz） |
| **自动重传** | 支持（最多 15 次） |
| **CRC 校验** | 1 或 2 字节 |

### 🔌 硬件连接

```
NRF24L01 模块
┌─────────────────────┐
│  VCC  ──────> 3.3V  │  供电（3.3V，不能接 5V！）
│  GND  ──────> GND   │  地
│  CE   ──────> PB8   │  芯片使能（控制发送/接收）
│  CSN  ──────> PB9   │  SPI 片选
│  SCK  ──────> PB13  │  SPI 时钟（SPI2_SCK）
│  MOSI ──────> PB15  │  SPI 主出从入（SPI2_MOSI）
│  MISO ──────> PB14  │  SPI 主入从出（SPI2_MISO）
│  IRQ  ──────> NC    │  中断（可选，本项目未使用）
└─────────────────────┘
```

**注意事项**：
- ⚠️ **电源必须 3.3V**：5V 会烧毁模块
- ⚠️ **稳定供电**：发射时电流突变，建议加 10μF 电容
- ⚠️ **天线方向**：垂直放置，避免遮挡

### 🔧 代码实现

#### 1. 依赖注入接口

```c
/* 定义总线接口结构体 */
typedef struct {
  monitoring_status_t (*transfer)(const uint8_t *tx, uint8_t *rx, uint16_t len);
  void (*chip_select)(uint8_t level);    // CSN 引脚控制
  void (*chip_enable)(uint8_t level);    // CE 引脚控制
  uint8_t ready;
} monitoring_nrf24_bus_t;

/* 初始化时绑定具体实现 */
monitoring_nrf24_bus_t nrf24_bus = {
  .transfer = MonitoringSpi_Transfer,              // SPI 传输函数
  .chip_select = MonitoringTasks_Nrf24ChipSelect,  // CSN 控制
  .chip_enable = MonitoringTasks_Nrf24ChipEnable,  // CE 控制
  .ready = 1U
};

MonitoringNrf24_Bind(&nrf24_bus);  // 绑定总线
MonitoringNrf24_Init();             // 初始化模块
```

**为什么这样设计？**
- ✅ **解耦**：NRF24 驱动不依赖具体 HAL 库
- ✅ **可测试**：可以用模拟接口进行单元测试
- ✅ **可移植**：换芯片只需实现新的接口函数

#### 2. 数据上报

```c
/* 在 ReportTask 中发送数据 */
#if MONITOR_NRF24_REPORT_ENABLED
  if (MonitoringNrf24_IsReady() != 0U) {
    // 1. 编码数据（压缩为 32 字节）
    nrf24_payload_length = MonitoringTasks_BuildNrf24Payload(
      &result, nrf24_payload);

    // 2. 无线发送
    nrf24_status = MonitoringNrf24_SendPayload(
      nrf24_payload, nrf24_payload_length);

    // 3. 统计
    if (nrf24_status != MONITOR_NRF24_OK) {
      g_health.nrf24_errors++;  // 发送失败计数
    } else {
      g_health.nrf24_reports++;  // 成功计数
    }
  }
#endif
```

#### 3. 数据包格式

由于 NRF24L01 单包最大 32 字节，需要**压缩数据**：

```c
/* 紧凑的二进制格式（约 28 字节） */
typedef struct {
  uint32_t cycle_id;              // 4 字节：周期 ID
  uint32_t timestamp;             // 4 字节：时间戳
  int16_t  temperature_centi;     // 2 字节：温度（×100）
  uint16_t current_ma;            // 2 字节：电流（mA）
  uint16_t vib_x_mg;              // 2 字节：振动 X 轴（mg）
  uint16_t vib_y_mg;              // 2 字节：振动 Y 轴（mg）
  uint16_t vib_z_mg;              // 2 字节：振动 Z 轴（mg）
  uint8_t  alert_mask;            // 1 字节：告警掩码
  uint8_t  alert_states;          // 1 字节：告警状态
  uint8_t  valid_mask;            // 1 字节：有效通道掩码
  uint8_t  reserved[5];           // 5 字节：保留
} __attribute__((packed)) nrf24_packet_t;  // 总计 28 字节
```

### 📊 双通道上报对比

您的系统使用**双通道上报**：

| 特性 | UART 串口 | NRF24L01 无线 |
|------|-----------|--------------|
| **用途** | 开发调试 | 实际部署 |
| **数据格式** | 文本（ASCII） | 二进制（紧凑） |
| **数据量** | 不限（详细日志） | 32 字节/包 |
| **实时性** | 立即输出 | ~5 ms 延迟 |
| **距离** | < 3 米（线长） | < 100 米 |
| **功耗** | 无额外功耗 | 发送 ~13 mA |
| **可靠性** | 100%（有线） | ~95%（重传机制） |
| **成本** | 0（MCU 自带） | ~5 元/模块 |

**日志示例对比**：

```
UART 输出（详细）：
[RTOS] cycle=1 state=EVALUATE valid=0x00000007 errors=0x00000000 
       temp=25.12°C current=120mA vib=15/18/12mg alert=0x00000000 
       process_ms=85

NRF24L01 输出（紧凑）：
[二进制] 01 00 00 00  // cycle_id = 1
         E4 1A 00 00  // timestamp = 6884
         D0 09        // temp = 2512 (25.12°C)
         78 00        // current = 120 mA
         0F 00        // vib_x = 15 mg
         12 00        // vib_y = 18 mg
         0C 00        // vib_z = 12 mg
         00           // alert_mask = 0
         00           // alert_states = 0
         07           // valid_mask = 0x07
```

### 🌐 组网示例

**星型拓扑**（一个接收器 + 多个发射器）：

```
监测节点 1 (地址 0x01) ──┐
监测节点 2 (地址 0x02) ──┤
监测节点 3 (地址 0x03) ──┼──> 接收节点 (地址 0x00)
监测节点 4 (地址 0x04) ──┤      ↓
监测节点 5 (地址 0x05) ──┘   上位机 (串口/WiFi)
                               ↓
                          云平台/数据库
```

**时分复用**：
- 节点 1：每 5 分钟发送一次（00:00, 00:05, ...）
- 节点 2：偏移 1 分钟（00:01, 00:06, ...）
- 节点 3：偏移 2 分钟（00:02, 00:07, ...）
- ...

避免冲突，提高可靠性。

---

## 为什么需要这些模块？

### 🎯 项目目标：低功耗分布式监测系统

| 需求 | 解决方案 | 模块 |
|------|---------|------|
| **测量电流** | 非侵入式、电气隔离 | ACS712 霍尔传感器 |
| **数字化** | 高速、自动、不占 CPU | ADC + DMA |
| **无线上报** | 低功耗、远距离、低成本 | NRF24L01 |
| **有线调试** | 详细日志、实时查看 | UART 串口 |

### 💡 典型应用场景

#### 场景 1：工厂设备监控
```
问题：10 台风扇分布在车间各处，如何监控？

传统方案：
❌ 拉线到每台设备（成本高、施工难）
❌ 人工巡检（效率低、不及时）

本项目方案：
✅ 每台设备安装一个监测节点（电池供电）
✅ 无线上报到中控室
✅ 异常实时告警
✅ 数据自动记录
```

#### 场景 2：电池续航优化
```
问题：电池供电节点如何运行更久？

分析：
- 运行模式：40 mA
- 无线发送：13 mA × 5 秒/300 秒 = 0.22 mA 平均
- Stop 模式：0.03 mA

平均功耗：(40 mA × 5s + 0.03 mA × 295s) / 300s ≈ 0.7 mA

1000 mAh 电池寿命：1000 / 0.7 ≈ 1400 小时 ≈ 58 天

如果没有低功耗设计：1000 / 40 = 25 小时 ≈ 1 天
```

#### 场景 3：数据压缩传输
```
问题：UART 日志有 200+ 字节，NRF24L01 只能传 32 字节？

解决：
✅ UART：详细文本日志（开发调试用）
✅ NRF24：压缩二进制（实际部署用）

只传关键数据：
- 周期 ID、时间戳
- 温度、电流、振动（各 2 字节）
- 告警状态（2 字节）
- 总共 ~28 字节，符合限制
```

---

## 总结

### 📌 核心概念

1. **ACS712 电流传感器 = 硬件传感器 + ADC 接口**
   - 传感器输出模拟电压（0-5V）
   - ADC 读取并转换为数字值（0-4095）
   - 软件计算得到实际电流（mA）

2. **NRF24L01 无线模块 = 远程数据传输**
   - 实现无线上报，适合分布式监测
   - 低功耗、低成本、易组网
   - 与 UART 互补（调试 vs 部署）

### 🔍 您的疑问解答

**Q1: 为什么看到的是 ADC 而不是电流传感器？**

A: 因为 ACS712 的**输出是电压**，需要 ADC 读取。这就像：
- 温度计输出的是水银柱高度（模拟量）
- 你用尺子测量水银柱（ADC）
- 根据刻度计算出温度（算法）

**Q2: 为什么要加无线模块？**

A: 因为实际应用中：
- ✅ 设备可能分散、移动、难以布线
- ✅ 电池供电，需要低功耗无线方案
- ✅ 一对多组网，一个接收器收集多个节点
- ✅ NRF24L01 是低功耗、低成本的最佳选择

### 📚 相关文件

- **配置**：`firmware/.../Core/Inc/monitoring_config.h`
- **驱动**：`firmware/.../Core/Src/monitoring_nrf24.c`
- **采集**：`firmware/.../Core/Src/monitoring_acquisition.c`
- **算法**：`firmware/.../Core/Src/monitoring_algorithm.c`
- **接线**：`hardware/引脚分配_暂定方案.md`

---

**💡 如果还有疑问，欢迎继续提问！**

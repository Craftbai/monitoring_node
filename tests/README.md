# 测试目录规划

后续按里程碑放置以下材料：

- `data/`：CSV 原始采样和对应的 Python 浮点特征结果。
- 正常、轻微异常、持续异常的离线回放样本。
- I2C/SPI 超时、传感器断开和 DMA 压力测试记录。
- RTC 唤醒和功耗测量结果。

建议先在 PC 上验证纯算法和告警状态机，再把同一份原始样本喂给 MCU 对照。测试记录必须注明是 PC、目标板、真实传感器还是整机长稳层级。

# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

低功耗多参数状态监测节点：在正点原子精英 STM32F103 开发板 V2（STM32F103ZET6/LQFP144）+ FreeRTOS 平台上，对低压直流风扇等安全演示对象进行振动、温度和电流的周期性状态评估。

**关键约束**：
- **不是连续在线系统**：采用 RTC 周期唤醒 → 采集固定窗口 → 处理/上报 → Stop 模式的间歇策略
- **安全边界**：仅采集低压直流或隔离传感器输出，不直接采样市电或工业电机；告警不接入生产停机联锁
- **首版范围**：周期性状态评估、特征提取、连续超限判定和结果上报，非实时保护系统

## 当前项目状态

**M1 已完成**：CubeIDE 工程已生成，目标为 STM32F103ZET6，`monitoring_node_f103ze.ioc`、HAL 驱动、启动文件和链接脚本已就位。

**M0 部分完成**：开发板和 MCU 已确定，但传感器型号、模拟前端、引脚分配、上报链路和实物板卡验证仍未冻结。

**不应直接编写的内容**：在传感器和引脚基线确认前，不编写绑定具体器件的正式驱动代码。

## 分层架构（docs/02_任务与数据流.md）

依赖方向只能从上层指向下层抽象：

| 层级 | 模块 | 职责 |
| --- | --- | --- |
| 应用与策略层 | `cycle_manager`、`alert_policy` | 周期状态机、连续超限判定 |
| 信号处理层 | `filter`、`feature_extractor`、`fft_analyzer` | 标定、RMS、峰峰值、频带能量 |
| 采集服务层 | `vibration_acq`、`temperature_acq`、`current_acq` | 统一采样窗口、时间戳、有效标志 |
| 设备驱动层 | SPI/I2C/ADC DMA/RTC/UART | 器件初始化、收发、超时、错误恢复 |
| 平台服务层 | FreeRTOS、日志、看门狗、配置 | 调度、诊断、参数快照 |
| BSP/HAL 层 | STM32CubeMX 生成代码 | 时钟、引脚、中断、外设适配 |

## 周期状态机

每次 RTC 唤醒生成唯一 `cycle_id`，状态流转：

`BOOT → SELF_TEST → IDLE → ACQUIRE → PROCESS → EVALUATE → REPORT → PREPARE_STOP → STOP → (RTC 唤醒)`

关键原则：
- 状态迁移由 `cycle_manager` 统一完成，驱动和算法只发布事件
- 每个周期无论成功或失败都必须到达明确的收尾状态
- 传感器断开、采集超时或算法溢出时，通过有效标志和错误码表达，不填零冒充正常值

## FreeRTOS 任务模型

无硬件 FPU，512 KB Flash、64 KB SRAM；所有缓冲、FFT 工作区、任务栈和队列共享同一份 SRAM 预算。

| 任务 | 建议优先级 | 职责 |
| --- | --- | --- |
| `acquisition_task` | 高 | 启动 SPI/FIFO、I2C 和 ADC DMA 窗口 |
| `processing_task` | 中高 | 使用 CMSIS-DSP 完成特征和 FFT |
| `cycle_task` | 中 | 应用状态拥有者、告警状态机 |
| `report_task` | 中低 | UART/无线结果、有界超时重试 |
| `health_task` | 低 | 心跳、栈水位、看门狗许可 |

**中断约束**：DMA/传感器中断只搬运缓冲索引和任务通知，不做浮点、FFT、日志格式化或阻塞 I/O。

## 数据对象与所有权

**采样块**：包含 `cycle_id`、`sequence`、`channel`、`sample_rate_hz`、`sample_count`、`timestamp_ticks`、标定参数、有效标志和固定长度样本数组。

- 驱动从块池取得空块并填写
- 采集任务把所有权交给处理队列（只传索引）
- 处理任务完成后归还块池
- 块池耗尽时记录丢弃，不覆盖仍被处理的数据

**周期结果**：必须区分"数值为零"和"通道无效"，包含配置版本、各通道特征、告警状态、连续超限计数、采集与处理耗时、错误位图。

## 算法与精度

- 振动：去直流、窗函数、RMS、峰峰值、峰值因子、FFT 频带能量
- 温度：周期读取标量、限幅、简单趋势，不做 FFT
- 电流：ADC 原始值、零点和比例标定、RMS 或均值（取决于直流/交流前端）

**验证要求**：Q15 FFT 必须与 Python 浮点参考对照，记录缩放、窗增益、饱和次数和误差容限。阈值只能作用于单位明确且有效的物理量。

## 告警状态机

`NORMAL → PENDING → ACTIVE → RECOVERING → NORMAL`

- 连续 N 个有效周期超限后进入 `ACTIVE`
- 任何无效周期都记录诊断，默认不计为正常，也不自动清除告警
- 恢复阈值与触发阈值分离形成迟滞；连续 M 个有效周期低于恢复阈值后清除

## 低功耗协调

进入 Stop 前必须满足：
1. DMA 和传感器采集已停止，外设中断标志已清理
2. 采样块已处理或明确丢弃，块池无悬空所有权
3. 上报成功或达到本周期截止时间，禁止无限重试
4. 保留状态已写入 RAM/备份域，RTC 唤醒源已配置
5. 日志发送完成或允许丢弃低优先级日志

**测量要求**：低功耗目标必须用仪器测量整周期平均电流和各状态驻留时间，不能仅引用芯片数据手册的典型 Stop 电流。

## 开发路线图（docs/05_开发路线图.md）

**推进原则**：按 M0 到 M7 顺序推进，当前阶段门禁未满足时，不同时接入全部硬件和 RTOS 功能。

| 阶段 | 目标 | 退出门禁 |
| --- | --- | --- |
| M0 基线冻结 | 固定开发板、传感器、前端、工具链 | 关键硬件项无"待确认" |
| M1 板级冒烟 | 跑通最小 MCU 工程和调试链 | 可下载、复位、UART 日志、RTC 唤醒、Stop |
| M2 单设备驱动 | 分别证明 SPI、I2C、ADC DMA | ID/读数/超时/恢复路径可复现 |
| M3 PC 算法参考 | 固定数据格式、特征公式和期望值 | 浮点结果和边界用例稳定 |
| M4 采集与 DSP | 建立固定窗口和 CMSIS-DSP 结果 | 无覆盖丢块，误差和饱和可解释 |
| M5 RTOS 闭环 | 接通状态机、告警、上报和健康监测 | 正常与故障周期均能收尾 |
| M6 低功耗闭环 | 完成周期唤醒、采集、上报和 Stop | 多周期稳定且实测功耗有证据 |
| M7 整机验收 | 在安全演示对象上完成场景与长稳 | 验证清单通过，结论边界明确 |

## 固件开发规范

**目录规划**：
- `Core/App/`：周期状态机、任务创建、告警策略
- `Core/Acquisition/`：采样块、块池、三类采集服务
- `Core/Algorithm/`：标定、滤波、特征、FFT
- `Core/Drivers/`：传感器及板级外设适配
- `Core/Platform/`：配置、日志、时间、错误码、看门狗
- `Core/Telemetry/`：UART 或无线结果编码与发送
- `AppTests/`：可脱离硬件运行的算法和状态机测试

**生成代码边界**：
- CubeMX 生成的 HAL、启动文件和中断框架保持可再生成
- 自编代码放在受控目录或官方用户代码区
- 不直接修改生成文件来隐藏业务逻辑

**资源约束**：
- 无硬件 FPU，热路径优先使用定点或降低浮点使用量
- 首版禁止在高频链路中反复 `malloc/free`，使用静态队列和固定块池
- 1024 点 Q15 窗口、DMA 双缓冲、FFT 临时区、任务栈和日志缓冲必须纳入同一份 SRAM 预算

## PC 侧开发环境

**算法参考与测试**：
```powershell
py -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r .\tools\requirements.txt
```

以上命令只准备 Python 分析环境（numpy、scipy、pandas、matplotlib、pytest），不生成或验证 MCU 固件。

**验证流程**：
1. 先在 PC 用 NumPy/SciPy 建立浮点参考和期望结果
2. 用 pytest 固化结果，避免 MCU 移植时边算边改定义
3. 将 PC 浮点参考逐项移植到 CMSIS-DSP
4. 用完全相同的原始样本对照 PC 与 MCU 输出

## 配置管理

`config/thresholds.example.json` 是参数含义和 PC 对照的样例，不要求 MCU 在首版运行时解析 JSON。

固件侧映射为版本化 `monitor_config_t`，并在启动和每次结果中输出配置版本与关键参数。

**配置检查**：
- 采样点数为 FFT 实现支持的长度，采样率覆盖目标频带
- 频带上下限满足 `0 <= low < high < sample_rate_hz / 2`
- 阈值单位与传感器标定单位一致
- 周期大于一次最坏采集、处理和上报总时长

## 错误与恢复原则

- I2C/SPI 超时有次数上限；先复位总线或器件，再把失败交给周期状态机
- DMA 溢出、块池耗尽和序号断裂必须计数，并使相关特征无效
- 单个非关键传感器可按已定义策略降级；关键通道不可用时本周期不输出"正常"
- 上报失败不应阻塞下一次休眠，但要保留累计计数
- 看门狗只处理软件失控，不代替业务超时和总线恢复
- 重启后记录复位原因、上个周期状态和配置版本

## 工程操作命令

### 固件构建（STM32CubeIDE）
1. 打开 `firmware/monitoring_node_f103ze/monitoring_node_f103ze.ioc` 用 CubeMX 配置外设
2. 在 CubeIDE 中 Project → Build Project 或使用工具栏构建按钮
3. 生成的 ELF 文件位于 `Debug/monitoring_node_f103ze.elf`

### 固件下载与调试
- 通过 ST-Link 连接 JTAG 4 pins（PA13/JTMS、PA14/JTCK、PA15/JTDI、PB3/JTDO、NRST）
- CubeIDE 中 Run → Debug 或使用工具栏调试按钮
- USART1（PA9/PA10）输出启动日志和运行状态

### Python 测试
```powershell
.\.venv\Scripts\python.exe -m pytest tests/  # 运行所有测试
```

## 重要文档

- `docs/01_需求与边界.md`：首版范围、安全限制和完成定义
- `docs/02_任务与数据流.md`：分层架构、模块职责、任务模型、状态机和接口契约
- `docs/03_物料与工具.md`：硬件选择条件、软件环境和仪器要求
- `docs/04_验证清单.md`：阶段门禁、异常场景和证据层级
- `docs/05_开发路线图.md`：从硬件基线到整机低功耗演示的实际开工顺序
- `firmware/STM32F103ZET6_CubeIDE完整配置教程.md`：从新建工程到外设配置的逐项说明
- `firmware/M1_板级冒烟配置.md`：第一次 CubeMX/CubeIDE 工程的配置和 ST-Link 接线
- `hardware/硬件基线.md`：开发板、MCU、传感器、引脚分配和工具链版本
- `hardware/引脚分配_暂定方案.md`：按精英板 V2 IO 表规划的引脚方案

## 关键约束与提醒

1. **硬件基线优先**：在 `hardware/硬件基线.md` 中传感器型号、引脚分配未冻结前，不编写绑定具体器件的正式驱动
2. **证据驱动**：只有在真实硬件上完成对应测试并保留证据后，才能将驱动、低功耗、算法精度或稳定性写成已实现能力
3. **阶段门禁**：当前阶段退出门禁未满足时，不同时接入全部硬件和 RTOS 功能
4. **算法验证**：Q15 FFT 必须与 Python 浮点参考对照，记录误差容限
5. **资源预算**：1024 点窗口、DMA 双缓冲、FFT 工作区、任务栈和日志缓冲必须通过链接映射与运行统计确定，不预设伪精确数值
6. **无效数据标记**：传感器断开、采集超时或算法溢出时，通过有效标志和错误码表达，不填零冒充正常值
7. **CubeMX 可再生成**：不直接修改 HAL 生成文件来隐藏业务逻辑，自编代码放在受控目录或用户代码区

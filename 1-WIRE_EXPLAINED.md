# DS18B20 与 1-Wire 协议详解

## 目录
1. [什么是 1-Wire？](#什么是-1-wire)
2. [为什么 DS18B20 不用 I2C/SPI？](#为什么-ds18b20-不用-i2cspi)
3. [1-Wire vs I2C vs SPI 对比](#1-wire-vs-i2c-vs-spi-对比)
4. [1-Wire 工作原理](#1-wire-工作原理)
5. [代码实现](#代码实现)

---

## 什么是 1-Wire？

**1-Wire** 是 Dallas Semiconductor（现为 Maxim Integrated，后被 ADI 收购）设计的**单总线通信协议**。

### 核心特点

```
传统串行通信（需要多根线）：
┌─────────┐  SCLK   ┌─────────┐
│  MCU    │ ──────> │ Sensor  │
│         │  MOSI   │         │
│         │ ──────> │         │
│         │  MISO   │         │
│         │ <────── │         │
│         │  GND    │         │
│         │ ──────> │         │
└─────────┘         └─────────┘
需要 4 根线：SCLK + MOSI + MISO + GND

1-Wire（只需 2 根线）：
┌─────────┐  DATA   ┌─────────┐
│  MCU    │ <────> │ DS18B20 │
│         │  GND    │         │
│         │ ──────> │         │
└─────────┘         └─────────┘
只需 2 根线：DATA + GND（甚至可以寄生供电，只用 1 根！）
```

### 🎯 1-Wire = 单根数据线 + 双向通信

**关键特性**：
- ✅ **只需 1 根数据线**（+ 1 根 GND）
- ✅ **双向通信**（主机和从机都可以在同一根线上发送/接收）
- ✅ **寄生供电**（可选）：甚至不需要 VCC 线
- ✅ **多设备挂载**：一条总线可以连接多个传感器
- ✅ **每个设备有唯一 64 位 ID**

---

## 为什么 DS18B20 不用 I2C/SPI？

### 🤔 设计目标不同

| 协议 | 设计目标 | 典型应用 |
|------|---------|---------|
| **SPI** | 高速数据传输（MHz 级） | Flash 存储器、显示屏、高速 ADC |
| **I2C** | 中速、多设备（kHz-MHz） | EEPROM、RTC、传感器集群 |
| **1-Wire** | **极简布线、低速、远距离** | **温度传感器、ID 芯片** |

### 📐 DS18B20 的应用场景

DS18B20 是一个**温度传感器**，它的需求是：

| 需求 | 1-Wire 的优势 |
|------|---------------|
| 🌡️ **温度变化慢** | 不需要高速传输（温度每秒变化小） |
| 📏 **远距离布线** | 可以用长线连接（50-100 米） |
| 💰 **成本敏感** | 只需 2 根线，节省连接器和线材 |
| 🔌 **简化接线** | 一条线连多个传感器（例如：监测多个房间温度） |
| ⚡ **寄生供电** | 某些场景不需要 VCC 线 |

### 💡 实际应用对比

#### 场景 1：监测 10 个房间的温度

**如果用 I2C**（需要 3 根线）：
```
每个传感器需要：SCL + SDA + GND
┌─────┐ SCL+SDA+GND ┌─────────┐
│ MCU ├─────────────┤ 房间1   │
│     ├─────────────┤ 房间2   │
│     ├─────────────┤ 房间3   │
│     │     ...     │  ...    │
│     ├─────────────┤ 房间10  │
└─────┘             └─────────┘
需要：3 × 10 = 30 根线
```

**如果用 1-Wire**（只需 2 根线）：
```
所有传感器共享一条总线：
┌─────┐ DATA+GND    ┌─────────┐
│ MCU ├─────────────┤ 房间1   │
│     │             ├─────────┤
│     │             │ 房间2   │
│     │             ├─────────┤
│     │             │ 房间3   │
│     │             │  ...    │
│     │             ├─────────┤
│     │             │ 房间10  │
└─────┘             └─────────┘
需要：2 根线（总线拓扑）
节省：28 根线！
```

#### 场景 2：嵌入式系统引脚稀缺

**您的项目（STM32F103ZET6）**：

| 协议 | 需要引脚 | DS18B20 使用 |
|------|---------|-------------|
| SPI | 4 根（SCLK/MOSI/MISO/CS） | ❌ 太浪费 |
| I2C | 2 根（SCL/SDA） | 🤔 可以，但已被 MPU6050 占用 |
| **1-Wire** | **1 根（DATA）** | ✅ **只用 1 根引脚！** |

您的引脚分配：
- I2C1：被 MPU6050 占用（PB8/PB9）
- SPI2：被 NRF24L01 占用（PB13/14/15）
- 1-Wire：只需要 **1 根引脚**（例如 PG11）

**节省的引脚可以用于其他功能！**

---

## 1-Wire vs I2C vs SPI 对比

### 📊 完整对比表

| 特性 | SPI | I2C | 1-Wire |
|------|-----|-----|--------|
| **引脚数** | 4（SCLK/MOSI/MISO/CS） | 2（SCL/SDA） | **1（DATA）** |
| **速度** | 高（MHz 级） | 中（kHz-MHz） | 低（16 kbps） |
| **距离** | 短（< 1 米） | 中（< 5 米） | **远（50-100 米）** |
| **同时设备数** | 1（每个 CS） | 127（7 位地址） | **无限（64 位 ID）** |
| **复杂度** | 高 | 中 | **低** |
| **成本** | 中 | 低 | **极低** |
| **典型应用** | Flash, LCD, ADC | EEPROM, RTC, Sensor | **温度传感器, ID** |
| **是否需要上拉** | 否 | 是（外部） | 是（外部） |
| **供电** | 必须独立供电 | 必须独立供电 | **可寄生供电** |

### 🎯 选择建议

```
选择 SPI：
✅ 需要高速传输（> 1 MHz）
✅ 传输大量数据
✅ 距离短（< 1 米）
❌ 引脚数量不是问题

选择 I2C：
✅ 中速传输（< 400 kHz）
✅ 多设备连接（同一总线）
✅ 节省引脚（只需 2 根）
❌ 距离短（< 5 米）

选择 1-Wire：
✅ 低速传输（温度等慢速信号）
✅ 极简布线（只需 1 根线）
✅ 远距离传输（50-100 米）
✅ 多设备总线连接
✅ 成本敏感应用
```

---

## 1-Wire 工作原理

### 📡 物理层

#### 硬件连接

```
标准供电模式（3 根线）：
┌─────────────┐         ┌─────────────┐
│   MCU       │         │  DS18B20    │
│             │         │             │
│  GPIO (PG11)├────┬────┤ DQ (数据)   │
│             │    │    │             │
│  VCC (3.3V) ├────┼────┤ VDD         │
│             │    │    │             │
│  GND        ├────┴────┤ GND         │
└─────────────┘         └─────────────┘
                 │
                 └─ 4.7kΩ 上拉电阻（必须！）

寄生供电模式（2 根线）：
┌─────────────┐         ┌─────────────┐
│   MCU       │         │  DS18B20    │
│             │         │             │
│  GPIO (PG11)├────┬────┤ DQ (数据)   │
│             │    │    │             │
│  GND        ├────┼────┤ VDD (接地!) │
│             │    │    │             │
│  GND        ├────┴────┤ GND         │
└─────────────┘         └─────────────┘
                 │
                 └─ 4.7kΩ 上拉电阻
                 
芯片从数据线"偷电"，存储到内部电容中
```

**为什么需要上拉电阻？**
- 1-Wire 是**开漏/开集输出**（Open-Drain）
- 总线默认被上拉电阻拉高（逻辑 1）
- 设备通过**拉低总线**（逻辑 0）来通信
- 释放总线后，上拉电阻自动恢复高电平

#### 电平定义

```
高电平（逻辑 1）：2.2V - 5.5V（上拉电阻维持）
低电平（逻辑 0）：0V - 0.8V（设备主动拉低）

时序单位：微秒（μs）
```

### 📝 协议层

#### 基本操作

1-Wire 通信由**时间槽（Time Slot）**组成，通过**脉冲宽度**编码数据：

```
写 0（60-120 μs 低电平）：
    ┌─────┐
    │     │
────┘     └──────────
    │<-60μs->│

写 1（1-15 μs 低电平）：
    ┌──┐
    │  │
────┘  └─────────────
    │<5μs>│

读位（主机拉低 1-15 μs，然后采样）：
    ┌──┐  采样
    │  │   ↓
────┘  └─┐ ┌────  （从机拉低 = 读到 0）
          └─┘
    │<5μs>│<15μs>│

    ┌──┐  采样
    │  │   ↓
────┘  └──────────  （从机释放 = 读到 1）
    │<5μs>│<15μs>│
```

#### 通信流程

**每次通信都需要 3 步**：

```
┌─────────────────────────────────────────────────┐
│ 步骤 1: 初始化（复位脉冲 + 存在脉冲）            │
├─────────────────────────────────────────────────┤
│ 主机：拉低 480 μs（复位脉冲）                   │
│ 从机：检测到复位后，拉低 60-240 μs（存在脉冲）  │
│                                                 │
│ ────────┐     ┌─────┐     ┌────────            │
│         │     │     │     │                    │
│         └─────┘     └─────┘                    │
│         主机复位    从机存在                     │
│         <480μs>   <60-240μs>                   │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│ 步骤 2: 发送命令（ROM 命令 + 功能命令）          │
├─────────────────────────────────────────────────┤
│ ROM 命令：                                      │
│   0xCC = Skip ROM（跳过 ROM，单设备总线）       │
│   0x55 = Match ROM（匹配 ROM，多设备总线）      │
│   0xF0 = Search ROM（搜索所有设备）             │
│                                                 │
│ 功能命令（DS18B20 专用）：                       │
│   0x44 = Convert T（开始温度转换）               │
│   0xBE = Read Scratchpad（读取数据）            │
│   0x4E = Write Scratchpad（写入配置）           │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│ 步骤 3: 数据交换                                 │
├─────────────────────────────────────────────────┤
│ 主机发送：逐位写入                               │
│ 主机接收：逐位读取                               │
│ 字节顺序：LSB first（低位在前）                  │
└─────────────────────────────────────────────────┘
```

### 🌡️ DS18B20 温度读取完整流程

```c
/* 伪代码示例 */

// 1. 初始化：复位脉冲 + 检测存在脉冲
if (!DS18B20_Reset()) {
  return ERROR;  // 没有检测到设备
}

// 2. 跳过 ROM（单设备模式）
DS18B20_WriteByte(0xCC);  // Skip ROM 命令

// 3. 启动温度转换
DS18B20_WriteByte(0x44);  // Convert T 命令

// 4. 等待转换完成（12 位精度需要 750 ms）
Delay(750);  // 或轮询直到总线变高

// 5. 再次初始化
if (!DS18B20_Reset()) {
  return ERROR;
}

// 6. 跳过 ROM
DS18B20_WriteByte(0xCC);

// 7. 读取数据
DS18B20_WriteByte(0xBE);  // Read Scratchpad 命令

// 8. 读取 9 个字节
uint8_t data[9];
for (int i = 0; i < 9; i++) {
  data[i] = DS18B20_ReadByte();
}

// 9. 解析温度（前 2 字节）
int16_t temp_raw = (data[1] << 8) | data[0];
float temperature = temp_raw / 16.0;  // 12 位精度：每 LSB = 0.0625°C
```

### 📊 时序图示例

```
完整的温度读取时序：

初始化
│
├─ 复位脉冲（480μs 低电平）
│  ────┐     ┌────
│      └─────┘
│      <480μs>
│
├─ 存在脉冲（从机回应）
│  ────┐ ┌───────
│      └─┘
│     <60-240μs>
│
发送命令 0xCC（Skip ROM）
│
├─ 写 0: ────┘     └────
├─ 写 0: ────┘     └────
├─ 写 1: ────┘  └───────
├─ 写 1: ────┘  └───────
├─ ... 共 8 位
│
发送命令 0x44（Convert T）
│
├─ 写 0: ────┘     └────
├─ 写 0: ────┘     └────
├─ 写 1: ────┘  └───────
├─ ... 共 8 位
│
等待 750ms（温度转换）
│
... 重复初始化和发送 0xCC ...
│
发送命令 0xBE（Read Scratchpad）
│
读取数据字节
│
├─ 读位: ────┘  └─┐ ┌── （从机控制）
├─ 读位: ────┘  └───── 
├─ ... 共 72 位（9 字节）
│
结束
```

---

## 代码实现

### 📁 您的项目实现

让我查看您的 DS18B20 驱动：

```c
/* 文件：monitoring_ds18b20.c */

// 1. 初始化（复位 + 存在检测）
monitoring_status_t DS18B20_Init(void) {
  // 发送复位脉冲
  GPIO_OUTPUT_LOW();   // 拉低
  Delay_us(480);       // 保持 480 μs
  
  GPIO_INPUT();        // 释放（变为输入，上拉电阻拉高）
  Delay_us(70);        // 等待从机回应
  
  uint8_t presence = GPIO_READ();  // 读取存在脉冲
  
  Delay_us(410);       // 等待存在脉冲结束
  
  return (presence == 0) ? MONITORING_OK : MONITORING_ERROR;
}

// 2. 写位
static void DS18B20_WriteBit(uint8_t bit) {
  GPIO_OUTPUT_LOW();   // 拉低开始时间槽
  
  if (bit) {
    // 写 1：短暂拉低后立即释放
    Delay_us(5);
    GPIO_INPUT();      // 释放（上拉电阻拉高）
    Delay_us(60);
  } else {
    // 写 0：持续拉低
    Delay_us(60);
    GPIO_INPUT();      // 释放
    Delay_us(5);
  }
}

// 3. 读位
static uint8_t DS18B20_ReadBit(void) {
  uint8_t bit;
  
  GPIO_OUTPUT_LOW();   // 拉低开始时间槽
  Delay_us(2);
  
  GPIO_INPUT();        // 释放（准备采样）
  Delay_us(10);        // 等待从机响应
  
  bit = GPIO_READ();   // 采样
  
  Delay_us(50);        // 等待时间槽结束
  
  return bit;
}

// 4. 读取温度
monitoring_status_t DS18B20_ReadTemperature(int16_t *temperature_centi) {
  uint8_t data[9];
  
  // 启动转换
  if (DS18B20_Init() != MONITORING_OK) return MONITORING_ERROR;
  DS18B20_WriteByte(0xCC);  // Skip ROM
  DS18B20_WriteByte(0x44);  // Convert T
  HAL_Delay(750);           // 等待转换（12 位精度）
  
  // 读取数据
  if (DS18B20_Init() != MONITORING_OK) return MONITORING_ERROR;
  DS18B20_WriteByte(0xCC);  // Skip ROM
  DS18B20_WriteByte(0xBE);  // Read Scratchpad
  
  for (int i = 0; i < 9; i++) {
    data[i] = DS18B20_ReadByte();
  }
  
  // 解析温度（16 位，LSB first）
  int16_t temp_raw = (data[1] << 8) | data[0];
  
  // 转换为 0.01°C 单位（项目统一格式）
  *temperature_centi = (temp_raw * 100) / 16;  // 12 位：每 LSB = 0.0625°C
  
  return MONITORING_OK;
}
```

### 🔧 GPIO 配置

```c
/* 1-Wire 需要 GPIO 可切换输入/输出 */

// 配置为输出（拉低总线）
void GPIO_OUTPUT_LOW(void) {
  GPIO_InitTypeDef GPIO_InitStruct = {0};
  GPIO_InitStruct.Pin = DS18B20_PIN;
  GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP;  // 推挽输出
  GPIO_InitStruct.Pull = GPIO_NOPULL;
  GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_HIGH;
  HAL_GPIO_Init(DS18B20_GPIO_PORT, &GPIO_InitStruct);
  
  HAL_GPIO_WritePin(DS18B20_GPIO_PORT, DS18B20_PIN, GPIO_PIN_RESET);  // 拉低
}

// 配置为输入（读取或释放总线）
void GPIO_INPUT(void) {
  GPIO_InitTypeDef GPIO_InitStruct = {0};
  GPIO_InitStruct.Pin = DS18B20_PIN;
  GPIO_InitStruct.Mode = GPIO_MODE_INPUT;      // 输入模式
  GPIO_InitStruct.Pull = GPIO_NOPULL;          // 外部上拉电阻
  HAL_GPIO_Init(DS18B20_GPIO_PORT, &GPIO_InitStruct);
}

// 读取引脚状态
uint8_t GPIO_READ(void) {
  return HAL_GPIO_ReadPin(DS18B20_GPIO_PORT, DS18B20_PIN);
}
```

### ⚠️ 注意事项

1. **上拉电阻必须！**
   - 典型值：4.7kΩ
   - 太小：功耗高
   - 太大：信号上升慢，通信不可靠

2. **时序严格！**
   - 必须使用微秒级延时（`Delay_us`）
   - 中断会破坏时序，建议临时关闭中断

3. **长线传输**
   - 线越长，上拉电阻越小（例如 50 米用 2.2kΩ）
   - 添加终端电阻可改善信号质量

4. **寄生供电限制**
   - 温度转换时需要强电流
   - 长线或多设备不推荐寄生供电

---

## 总结

### 🎯 核心要点

1. **1-Wire ≠ I2C ≠ SPI**
   - 三者是完全不同的通信协议
   - 各有优缺点和适用场景

2. **DS18B20 为什么用 1-Wire？**
   - ✅ 只需 1 根引脚（节省资源）
   - ✅ 远距离布线（50-100 米）
   - ✅ 多设备总线（一条线接多个传感器）
   - ✅ 可选寄生供电
   - ❌ 速度慢（但温度变化慢，够用）

3. **您的项目中**
   - MPU6050：I2C（振动需要中速传输）
   - NRF24L01：SPI（无线需要高速）
   - DS18B20：**1-Wire**（温度慢速，节省引脚）
   - 三者互不冲突，各司其职！

### 📚 相关文件

- **驱动实现**：`firmware/.../Core/Src/monitoring_ds18b20.c`
- **头文件**：`firmware/.../Core/Inc/monitoring_ds18b20.h`
- **引脚配置**：`hardware/引脚分配_暂定方案.md`

---

**💡 还有疑问吗？欢迎继续提问！**
